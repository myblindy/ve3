<Window x:Class="ve3.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ve3.Views"
        mc:Ignorable="d"
        x:Name="RootWindow"
        Title="MainView" Height="450" Width="800">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top">Queued Frames: <Run Text="{Binding QueuedFramesCount, ElementName=RootWindow}"/></TextBlock>
        <Slider DockPanel.Dock="Top" Maximum="{Binding VideoDuration, ElementName=RootWindow}" Value="{Binding VideoPosition, ElementName=RootWindow}"/>
        <Image x:Name="VideoFrameImage"/>
    </DockPanel>
</Window>
